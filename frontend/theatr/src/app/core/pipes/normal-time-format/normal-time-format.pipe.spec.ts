import { NormalTimeFormatPipe } from './normal-time-format.pipe';

describe('NormalTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
