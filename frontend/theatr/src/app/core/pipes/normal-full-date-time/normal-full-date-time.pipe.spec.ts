import { NormalFullDateTimePipe } from './normal-full-date-time.pipe';

describe('NormalFullDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalFullDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
